This is how to setup the server to run unit tests or a Flask test server for
manual testing.

Run the following from the project root:
```
$ python -m venv venv
$ . venv/bin/activate
$ pip install flask
```
